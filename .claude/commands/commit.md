Read [docs/workflows/commit.md](../../docs/workflows/commit.md) and follow it exactly.

That file is the canonical, tool-agnostic procedure and is the single source of truth for how
this repository commits. This command is only a pointer to it, so that Claude Code, other AI
agents, and people all follow the same rules.

Do not follow a remembered version of the procedure. Read the file.
